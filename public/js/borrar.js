// var geometry4 = new THREE.BoxGeometry(1, 1, 1);
// var material4 = new THREE.MeshBasicMaterial({
//   color: 0x0000ff,
//   wireframe: true,
// });
// var cubemelo = new THREE.Mesh(geometry4, material4);
// //   cube.position.y = 2;
// scene.add(cubemelo);

// var mouse = new THREE.Vector2();

// let dsd = function (e) {
//   //   e.preventDefault();
//   let rect = e.target.getBoundingClientRect();
//   mouse.x = ((e.clientX - rect.left) / canvas.offsetWidth) * 2 - 1;
//   mouse.y = -((e.clientY - rect.top) / canvas.clientHeight) * 2 + 1;

//   cubemelo.position.set(mouse.x, mouse.y, 0);
// };

// dsd.bind(canvas);

// canvas.addEventListener('mousemove', dsd);
